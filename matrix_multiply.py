
#def matrix_multiply(matrix1: list[list], matrix2: list[list]) -> list:
#    matrix1_row=len(matrix1)
#    matrix1_column=(lambda matrix1=matrix1: len(matrix1[0]) if matrix1_row>0 else 0)
#    matrix2_row = len(matrix2)
#    matrix2_column = (lambda matrix2=matrix2: len(matrix2[0]) if matrix2_row>0 else 0)

#    if matrix1_column == matrix2_row :
#       result = [[0 for x in range(matrix2_column)] for y in range(matrix1_row)]
#       result +=
#       result =list(map( reduce(map(lambda x , y : x*y , matrix1[i][k] , matrix2[k][j] )),
#                     i=list(range(matrix1_row)), j=list(range(matrix2_column) , k=list(range(matrix2_row)))))

mat_col=
